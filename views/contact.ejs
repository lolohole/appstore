<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Contact Me</title>
  <link rel="stylesheet" href="/css/styles.css" />
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f9f9f9;
      margin: 0;
      padding: 0;
      color: #333;
    }

    .contact-container {
      max-width: 700px;
      margin: 80px auto;
      background: #fff;
      padding: 40px 50px;
      border-radius: 15px;
      box-shadow: 0 6px 25px rgba(0, 0, 0, 0.1);
    }

    h1 {
      font-size: 44px;
      margin-bottom: 25px;
      color: #222;
      text-align: center;
    }

    p.intro {
      font-size: 18px;
      margin-bottom: 40px;
      text-align: center;
      color: #555;
    }

    form {
      display: flex;
      flex-direction: column;
    }

    label {
      font-weight: 600;
      margin-bottom: 8px;
      font-size: 16px;
    }

    input[type="text"],
    input[type="email"],
    textarea {
      padding: 12px 15px;
      margin-bottom: 25px;
      border-radius: 8px;
      border: 1.5px solid #ccc;
      font-size: 16px;
      transition: border-color 0.3s ease;
      resize: vertical;
    }

    input[type="text"]:focus,
    input[type="email"]:focus,
    textarea:focus {
      border-color: #007bff;
      outline: none;
    }

    textarea {
      min-height: 130px;
    }

    button {
      background-color: #007bff;
      border: none;
      padding: 16px;
      font-size: 18px;
      color: white;
      font-weight: 700;
      border-radius: 10px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #0056b3;
    }

    /* Responsive */
    @media (max-width: 480px) {
      .contact-container {
        padding: 30px 20px;
        margin: 40px 20px;
      }

      h1 {
        font-size: 32px;
      }
    }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <div class="contact-container">
    <h1>Contact <span style="color:#007bff;">Me</span></h1>
    <p class="intro">Feel free to reach out for collaborations, questions, or just to say hello! I'll get back to you as soon as possible.</p>

    <form action="/contact" method="POST">
      <label for="name">Full Name</label>
      <input type="text" id="name" name="name" placeholder="Your full name" required />

      <label for="email">Email Address</label>
      <input type="email" id="email" name="email" placeholder="Your email address" required />

      <label for="message">Message</label>
      <textarea id="message" name="message" placeholder="Write your message here..." required></textarea>

      <button type="submit">Send Message</button>
    </form>
  </div>
</body>
</html>
